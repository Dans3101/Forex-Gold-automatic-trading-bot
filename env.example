# Exact WhatsApp group name
GROUP_NAME=PocketOption Signals

# Binance symbols (comma-separated). Use crypto pairs supported by Binance.
SYMBOLS=BTCUSDT,ETHUSDT

# How often to scan (seconds)
LOOP_SECONDS=120

# Time between "incoming" notice and decision (ms)
PRE_SIGNAL_DELAY_MS=30000